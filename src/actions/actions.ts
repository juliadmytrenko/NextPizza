'use server';

// import { prisma } from "@/lib/prisma";

// export async function createIngredient(name: string) {
//     await prisma.ingredient.create({
//         data: {
//             name,
//         }
//     });
// }

// USE these functions in api route files instead